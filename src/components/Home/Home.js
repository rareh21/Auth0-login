import React from 'react';
import { Typography, Box } from '@material-ui/core';

const Home = () => {
    return(
        <Box py={10} px={20}>
          <Typography variant='h2' align='center'>You are on home page</Typography>
        </Box>
    );
}

export default Home;